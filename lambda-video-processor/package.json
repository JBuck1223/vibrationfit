{
  "name": "lambda-video-processor",
  "version": "1.0.0",
  "description": "AWS Lambda function to automatically process videos with MediaConvert",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@aws-sdk/client-mediaconvert": "^3.0.0"
  }
}

