'use client'

// Re-export FileUpload from the design system for backward compatibility
// The FileUpload component has been migrated to the design system
export { FileUpload } from '@/lib/design-system'
