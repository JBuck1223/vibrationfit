# Page Building Rules

**Last Updated:** August 30, 2026  
**Status:** Active

Canonical agent rules live in:

- `rules/PAGE_BUILDING_RULES.md`
- `rules/STUDIO_PAGE_BUILDING_RULES.md`
- `.cursor/rules/page-chrome.mdc`

The old “Container + Stack + boxed PageHero on every page” standard is retired. Studios use AreaBar. Standalone pages use a slim left-aligned `PageHero` or an `h1`.
