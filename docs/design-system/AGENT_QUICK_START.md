# Agent Quick Start — Building Pages

Canonical copy: `rules/AGENT_QUICK_START.md`

Studios (Admin, Life Vision, Journal, …): AreaBar in the layout, **no PageHero** on the page.

Standalone pages: slim left-aligned `PageHero` or an `h1`. Never a boxed gradient hero.
