# Page Building Rules

**Last Updated:** August 30, 2026  
**Status:** Active

Canonical agent rules live in:

- `rules/PAGE_BUILDING_RULES.md` — studio vs standalone chrome
- `rules/STUDIO_PAGE_BUILDING_RULES.md` — AreaBar layouts
- `.cursor/rules/page-chrome.mdc` — always-on agent rule

**PageHero is a slim title row, not a boxed gradient hero. Studio pages (including admin) use AreaBar and must not add PageHero.**
